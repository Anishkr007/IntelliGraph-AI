# AI Research & Evaluation Assistant — Dependencies
# =====================================================
# Install: pip install -r requirements.txt

# ── LangChain / LangGraph ─────────────────────────────────────────────────────
langchain>=0.2.16
langchain-core>=0.2.38
langchain-groq>=0.1.9
langgraph>=0.2.14

# ── FastAPI Backend ───────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── Streamlit Frontend ────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Search Tools (all free, no API keys needed) ───────────────────────────────
duckduckgo-search>=6.0.0        # Free web search via DuckDuckGo
wikipedia>=1.4.0                # Wikipedia search

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # .env file loading
pydantic>=2.7.0                 # Structured output schemas
httpx>=0.27.0                   # Async HTTP (used internally by FastAPI)
requests>=2.31.0                # HTTP client for Streamlit -> FastAPI calls
